import { StyledText, View } from "@aurora/components";

export function TransactionHistory() {
	return (
		<View backgroundColor={"$negative500"}>
			<StyledText variant="headingM">
				Transaction History not implemented on native
			</StyledText>
		</View>
	);
}
